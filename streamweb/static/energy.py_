import streamlit as st
import datetime
import pytz
from utils.metrics import log_runtime

long_title = "Energy"
short_title = "energy"
key = 9997
content_date = datetime.datetime(2022, 10, 1).astimezone(pytz.timezone("US/Eastern"))


@log_runtime
def render(location: st):

    location.header(f"{long_title}")

    import numpy as np
    import pandas as pd

    chart_data = pd.DataFrame(
        np.random.randn(20, 3),
        columns=['a', 'b', 'c'])

    st.line_chart(chart_data)
    

